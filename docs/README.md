# Cold Clear 2 - WebAssembly Version

This directory contains the WebAssembly build of Cold Clear 2, intended for use with GitHub Pages.

To test this locally, run a simple HTTP server in this directory. For example:

```bash
python -m http.server
```

Then open your browser to `http://localhost:8000`.
